"""API routers, one module per resource. All routers are mounted onto the
FastAPI app in main.py; this package holds no app-level configuration
(middleware, CORS, session secret) — that stays in main.py."""
